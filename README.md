TypeScript Check Value `null` or `undefined` Demo
=================================================

```
npm install
npx ts-node hello.ts
```

```
a == null
a is undefined (typeof(a) === 'undefined')
b == null
b is null (b === null)
```
